# Browser Phone Gift

Open `index.html` directly in a browser, or run a local static server from the repo root:

```bash
python3 -m http.server 4173 --directory browser-phone
```

Then visit:

```text
http://127.0.0.1:4173
```
