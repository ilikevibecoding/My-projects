# My-projects

This project animates X-wings and TIE fighters across a starry background.

## Usage
Open `index.html` in your browser to view the animation.

## Publishing your work
Add a remote and push your branch:

```bash
git remote add origin <url>
git push -u origin work
```
