// Authored landmarks: signature tree, ruins, fallen logs, roots, log bridge…
// (filled in by the level-design pass)

export function createLandmarks(ctx) {
  void ctx;
  return {
    update() {},
    applyQuality() {},
  };
}
