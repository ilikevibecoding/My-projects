// Ground micro-props: pebbles, twigs, roots and leaf drifts on and beside trails.
// (Stub — filled in by the ground pass.)

export function createGroundDetail(ctx) {
  return {
    update() {},
    applyQuality() {},
    stats: { instances: 0, triangles: 0 },
  };
}
