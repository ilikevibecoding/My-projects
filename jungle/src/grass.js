// Near-field grass: dense, dynamically streamed tufts around the camera.
// (Stub — filled in by the grass pass.)

export function createGrass(ctx) {
  return {
    update() {},
    applyQuality() {},
    stats: { instances: 0, triangles: 0 },
  };
}
